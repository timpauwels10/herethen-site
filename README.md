# herethen-site

Cloudflare Pages source for [herethen.net](https://herethen.net).

Single static `index.html` + `favicon.svg` at the repo root, deployed automatically on push to `main`. No build step.
